//----------------------------------------------

export type Key = {
	value: string;
	disabled?: boolean;
	wide?: boolean;
};

export enum ModalState {
	Login,
	Register,
	Closed,
}

//----------------------------------------------
